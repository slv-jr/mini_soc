# Inventaire Mini-SOC.
# Par défaut : déploiement local (le PC Ubuntu est sa propre cible).
# Transposable à un parc : ajouter d'autres hôtes sous [minisoc].

[minisoc]
localhost ansible_connection=local

[minisoc:vars]
ansible_python_interpreter=/usr/bin/python3
