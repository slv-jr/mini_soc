{
  "uid": "minisoc-net",
  "title": "Mini-SOC — Network Overview",
  "tags": ["minisoc", "network"],
  "timezone": "browser",
  "schemaVersion": 39,
  "refresh": "30s",
  "time": { "from": "now-6h", "to": "now" },
  "panels": [
    {
      "id": 1,
      "type": "timeseries",
      "title": "Trafic total (bytes/intervalle)",
      "datasource": { "type": "influxdb", "uid": "minisoc-influx" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "targets": [
        {
          "refId": "A",
          "query": "from(bucket: \"network_metrics\")\n  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |> filter(fn: (r) => r._measurement == \"network_traffic\" and r._field == \"bytes\")\n  |> aggregateWindow(every: v.windowPeriod, fn: sum, createEmpty: false)"
        }
      ]
    },
    {
      "id": 2,
      "type": "piechart",
      "title": "Répartition par protocole",
      "datasource": { "type": "influxdb", "uid": "minisoc-influx" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "targets": [
        {
          "refId": "A",
          "query": "from(bucket: \"network_metrics\")\n  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |> filter(fn: (r) => r._measurement == \"network_traffic\" and r._field == \"packets\")\n  |> group(columns: [\"protocol\"])\n  |> sum()"
        }
      ]
    },
    {
      "id": 3,
      "type": "table",
      "title": "Top talkers (par IP source)",
      "datasource": { "type": "influxdb", "uid": "minisoc-influx" },
      "gridPos": { "h": 9, "w": 24, "x": 0, "y": 8 },
      "targets": [
        {
          "refId": "A",
          "query": "from(bucket: \"network_metrics\")\n  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)\n  |> filter(fn: (r) => r._measurement == \"network_traffic\" and r._field == \"bytes\")\n  |> group(columns: [\"src_ip\"])\n  |> sum()\n  |> sort(columns: [\"_value\"], desc: true)\n  |> limit(n: 15)"
        }
      ]
    }
  ]
}
