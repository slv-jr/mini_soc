# Mini-SOC — dépendances Python (moteur de corrélation / SOAR / dashboard)
# Installation : pip install -r requirements.txt
# NB : la capture réseau (Scapy) n'est plus nécessaire — Suricata/Zeek s'en chargent.

# Sondes actives
icmplib==3.0.4
httpx==0.27.0

# Pipeline & stockage
redis==5.0.4
influxdb-client==1.43.0
PyYAML==6.0.2

# Enrichissement threat intel
geoip2==4.8.0

# Détection / exemple statistique
numpy==2.2.6

# Dashboard & API
flask==3.0.3
flask-cors==6.0.1

# Utils
python-dateutil==2.9.0
